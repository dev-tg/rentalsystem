angular.module('RENTAL')

    .controller('homeCtrl', function ($scope, $http, AUTHURLS, USER_MANAGE_URL, LOGINURL,toastr,$ocLazyLoad,$state,$rootScope) {
      
        document.title = "Rental System";

                
    
            }
        );

